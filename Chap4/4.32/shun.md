遅延リストの方が遅延度が高いというのは、遅延リストはcarも遅延されるということを指している。

例えば、遅延リストではこのようなゼロ除算を回避して定義できる。

```scheme
(cons (/ 1 0) 2)
```

ストリームでは、carについては遅延されないためエラーになる。

```scheme
(cons-stream (/ 1 0) 2)
```

この遅延度は、先頭に重い処理が載ってくる時などに利用できる。

```scheme
(cons (fib 99999) 1)
```